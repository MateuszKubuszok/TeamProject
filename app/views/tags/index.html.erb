<h1>Listing tags</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

<% @tags.each do |tag| %>
  <tr>
    <td><%= link_to(tag.name, tag_path(tag.name)) %></td>
  </tr>
<% end %>
</table>

<br />

<%= paginate @tags, params: { action: 'index', controller: 'tags' } %>